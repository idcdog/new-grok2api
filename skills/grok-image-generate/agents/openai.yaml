interface:
  display_name: "Grok Image Generate"
  short_description: "Generate images with a configured Grok API"
  default_prompt: "Use $grok-image-generate to generate an image with the configured Grok image API."

policy:
  allow_implicit_invocation: true
